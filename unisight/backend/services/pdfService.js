import PDFDocument from 'pdfkit';export const generateStudentReport = (user, marks, attendance, insight, res) => {  const doc = new PDFDocument({ margin: 40 });  res.setHeader('Content-Type', 'application/pdf');  res.setHeader('Content-Disposition', `attachment; filename="report-${user?.studentId || 'student'}.pdf"`);    doc.pipe(res);   doc.fontSize(22).fillColor('#4F46E5').text('UniSight Performance Report', { align: 'center' });  doc.moveDown(0.5);  doc.fontSize(12).fillColor('#4B5563').text(`Student: ${user?.name || 'N/A'}`, { align: 'center' });  doc.text(`Department: ${user?.department || 'N/A'} | ID: ${user?.studentId || 'N/A'}`, { align: 'center' });  doc.text(`Date of Generation: ${new Date().toLocaleDateString()}`, { align: 'center' });  doc.moveDown(2);  if (insight) {    doc.fontSize(16).fillColor('#1F2937').text('Longitudinal Academic Standing');    doc.moveDown(0.5);        doc.fontSize(12).fillColor('#374151');    doc.text(`Current CGPA: ${insight.cgpa || 'N/A'} / 10.0`);    doc.text(`Dropout Probability Score: ${insight.dropoutProbabilityScore !== null ? insight.dropoutProbabilityScore + '%' : 'N/A'}`);    doc.text(`Tier Classification: ${insight.dropoutTier}`);    doc.text(`Participation Metric: ${insight.participationScore || 'N/A'}`);    doc.text(`Consecutive Semesters At-Risk: ${insight.consecutiveAtRiskSemesters}`);    doc.moveDown(0.5);    doc.fillColor('#DC2626').text(`Flag Reason: ${insight.riskReason || 'None recorded'}`);    doc.moveDown(1.5);  }  doc.fontSize(16).fillColor('#1F2937').text('AI Generated Recommendations');  doc.moveDown(0.5);  if (insight?.recommendations?.length) {    insight.recommendations.forEach(r => {      doc.fontSize(11).fillColor('#B91C1C').text(`[${r.priority}] ${r.title}`);      doc.fontSize(10).fillColor('#4B5563').text(r.description);      doc.moveDown(0.5);    });  } else {    doc.fontSize(10).fillColor('#6B7280').text('No immediate action required.');  }  doc.moveDown();  if (insight?.resources?.length) {    doc.fontSize(14).fillColor('#10B981').text('Recommended Learning Resources');    insight.resources.forEach(res => {      doc.fontSize(10).fillColor('#4B5563').text(`• ${res.subject}: ${res.resource} (${res.type})`);    });  }    doc.end();};